{
  "$comment": "@generated by `just gen-schemas` (tools/codegen) from the Rust protocol types in crates/plugin-api/src/lib.rs; do not edit by hand.",
  "$id": "https://raw.githubusercontent.com/jag-k/clipboard-transformer/main/plugins/manifest.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": true,
  "definitions": {
    "CapabilityRequest": {
      "description": "A capability requested in the manifest with a human-readable reason.",
      "oneOf": [
        {
          "properties": {
            "kind": {
              "enum": [
                "http"
              ],
              "type": "string"
            },
            "reason": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "properties": {
            "kind": {
              "enum": [
                "env-expansion"
              ],
              "type": "string"
            },
            "reason": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "RuleDescriptor": {
      "description": "One rule type provided by a plugin.",
      "properties": {
        "description": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "examples": {
          "default": [],
          "description": "Example rule configurations rendered by the host.",
          "items": true,
          "type": "array"
        },
        "formats": {
          "default": [],
          "description": "Accepted clipboard formats in priority order. Empty means plain text.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "name": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "settings_schema": {
          "default": null,
          "description": "Optional JSON Schema for this rule's settings."
        },
        "type": {
          "description": "Local rule type name; the config-facing id is `<plugin-id>/<type>`.",
          "pattern": "^[A-Za-z0-9._-]+$",
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    }
  },
  "description": "Static identity and integration metadata embedded in the plugin module.",
  "properties": {
    "$schema": {
      "description": "Optional editor schema hint. Ignored and not retained by the runtime.",
      "format": "uri",
      "type": "string"
    },
    "api_version": {
      "const": 1,
      "description": "Plugin API version this module implements.",
      "format": "uint32",
      "minimum": 0.0,
      "type": "integer"
    },
    "author": {
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "capabilities": {
      "default": [],
      "description": "Capabilities the plugin asks for. Effective capabilities are the intersection of this list and the grants in the user config.",
      "items": {
        "$ref": "#/definitions/CapabilityRequest"
      },
      "type": "array"
    },
    "description": {
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "homepage": {
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "id": {
      "description": "Stable namespaced plugin id, e.g. `dev.jag-k.gitlab`.",
      "pattern": "^[A-Za-z0-9._-]+$",
      "type": "string"
    },
    "instructions": {
      "default": null,
      "description": "Structured setup instructions rendered by the host.",
      "type": [
        "string",
        "null"
      ]
    },
    "license": {
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "name": {
      "description": "Human-readable display name.",
      "pattern": "\\S",
      "type": "string"
    },
    "rules": {
      "description": "Rule types provided by this plugin.",
      "items": {
        "$ref": "#/definitions/RuleDescriptor"
      },
      "minItems": 1,
      "type": "array"
    },
    "settings_schema": {
      "default": null,
      "description": "Optional JSON Schema for the plugin's opaque `settings` value."
    },
    "version": {
      "description": "Plugin package version.",
      "type": "string"
    }
  },
  "required": [
    "api_version",
    "id",
    "name",
    "rules",
    "version"
  ],
  "title": "PluginManifest",
  "type": "object"
}
